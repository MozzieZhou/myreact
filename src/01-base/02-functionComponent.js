/**
 * 函数组件
 * @returns 
 */
function App() {
    return (
        <div className='app' id='appRoot'>
            <h1 className='title'>函数组建欢迎来到React世界</h1>
        </div>
    )
}

/**
 * 16.8 之前，无状态组件（无法做到累加等操作
 * 16.8 之后，react hooks
 */
export default App